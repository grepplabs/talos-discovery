name: Build

on:
  push:
    branches:
      - "**"
    tags-ignore:
      - '**'
    paths-ignore:
      - 'README.md'
      - "test/**"
      - "docs/**"
  pull_request:
    paths-ignore:
      - 'README.md'
      - "test/**"
      - "docs/**"
jobs:
  verify-code:
    uses: ./.github/workflows/_verify-code.yaml
  publish-image:
    uses: ./.github/workflows/_publish.yaml
    needs:
      - verify-code
    with:
      push_image: true
      go_releaser: false
